(library
    (name meta)
    (public_name dkmeta)
    (modules dkmeta quoting)
    (libraries dedukti.kernel dedukti.api dedukti.parsers))

(executable
    (name main)
    (public_name dkmeta)
    (modules main)
    (libraries dedukti.api dkmeta))
